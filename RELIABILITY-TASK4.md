# Task #4 · Judge Reliability Hardening —— 诊断报告

日期：2026-09-20　范围：只做"把错误答案被判对的概率压到接近 0"。未 push。
未改 prefetch / storage，未动 UI 功能（只关掉了一处难度展示覆盖）。

---

## 0. 一句话结论

**错误答案被判对这件事，从"概率很低"变成了"结构上不可能"。**

现在能返回 `correct: true` 的路径只剩**一条**：确定性引擎判 `equivalent`。
模型那条路按新规则只采信 `not_equivalent`（判错），永远产不出 `correct: true`。
所以 `wrong_answer_accepted = 0` 不再依赖模型表现，只依赖"线上跑的是这一版"。

同时发现了一个比判题更要紧的问题：**41 道已放行题里有 4 道（约 10%）参考答案本身是错的**，
引擎此前读不懂这些题的形态，保守地拒判，闸门于是放它们过去了。详见 §7。

---

## 1. 改动文件

| 文件 | 改了什么 |
| --- | --- |
| `math-quality.js` | 引擎（唯一真源）。裸 `\frac` 速写、`|A|` 绝对值、括号剥离的上下文判断、`judgeDeterministic`、`trustModelVerdict` |
| `cloudbase/deepseek/math-quality.js` | 引擎副本，`npm run sync:engine` 同步，逐字节一致 |
| `cloudbase/deepseek/index.js` | **线上真正跑的那份后端**。判题确定性层换 `judgeDeterministic`；模型层改 `trustModelVerdict`；响应新增 `judge_layer` / `detail` |
| `api/deepseek.js` | Vercel 那份副本，同样改动（`npm run check:sync` 强制两份业务段逐字节一致，不改就红） |
| `app.js` | 浏览器里的本地判题同步收紧；模型说"等价"不得判对；`judge_uncertain` 文案；`questionDifficultyLabel` 去掉 `displayDifficulty` |
| `tests/math-engine.cjs` | 反转"已知缺口"那条；新增 Task #4 回归（11 用例、裸 frac、方向、边界、模型权限） |
| `tests/pipeline-v3.cjs` | `UNCERTAIN` 夹具换掉；新增"模型说等价不采信"；难度展示断言反转 |
| `tests/reliability.cjs` | 夹具换成机器真的判不了的答案 |
| `tests/smoke-matrix.cjs` | 探针集守卫扩到全形态；新增 Task #4 不变式守卫 |
| `tools/smoke/probes.mjs` | **新**。探针集（11 条错答 + 6 条合法古怪） |
| `tools/smoke/judge-probe-local.mjs` | **新**。不碰网络的判定压力测试 |
| `tools/smoke/run.mjs` | 接入新探针集；采集 `judge_layer`；报告新增判定层分布与部署指纹 |
| `package.json` | 新增 `npm run probe:local` |

测试：`npm test` → **147 / 147 通过**（改造前 134 条；新增 Task #4 回归，
其中 3 条是把旧的"已知缺口/已知行为"断言反转过来的）。

---

## 2. parser 新增能力（A）

先说一个和预期不同的事实：**11 个用例里，10 个原来的 parser 就已经能解析**。
真正卡住的不是分层结构（`expression → unary → product → atom` 早就存在），
而是 LaTeX 前端的两处缺口。逐条实测结果：

| 用例 | 改动前 | 改动后 |
| --- | --- | --- |
| `-(1/3)` `-(-1/3)` `-(2x)` | 已能解析 | 不变 |
| `2(1/6)` `2\left(1/6\right)` | 已能解析 | 不变 |
| `2\cdot\frac{1}{6}` `2\times\frac{1}{6}` | 已能解析 | 不变 |
| `\left(-\frac{1}{3}\right)` `3(x+1)` `-2(x-1)` | 已能解析 | 不变 |
| **`\frac12(x+1)`** | **解析失败** | **修好** |

### 改动一：裸 `\frac` 速写

`\frac` 的参数改成"花括号组 **或** 单个 token"（`readFracArg`），
所以 `\frac12` = `(1)/(2)`、`\frac34x` = `(3)/(4)·x`、`\frac12(x+1)` = `(1)/(2)·(x+1)`。
按 LaTeX 语义每个参数只取**一个** token —— 读成数字串会把 `\frac12` 变成 `12/…`，那是静默算错。
`\frac1\pi` 这类读不出来的（第二个参数是命令）**一律不动**，保持 unparseable → `uncertain`。

### 改动二：`|A|` → `abs(A)`（11 个用例之外，但同类）

`|` 在 tokenizer 里是非法字符，于是 **`\ln|x|+C` 这种最常见的积分答案整条解析不了**。
这不是边角：`integral` 模块 16 道题里有 2 道的答案含 `|`，其中 1 道因此完全无法判定。
配对规则刻意保守：竖线不成对、或配对内容为空（`||x|-1|` 这种嵌套）→ **整段不动**，
仍旧 unparseable → `uncertain`。嵌套绝对值在 LaTeX 里本身就有歧义，去猜只会猜错。

### 改动三：括号剥离不再凭空造数

`normalize()` 里把 `(a)/(b)` 清成 `a/b` 的那条规则**不看上下文**，
于是 `2(1)/(6)` 被清成 `21/6`（一个捏造出来的数），
更糟的是 `1(2)/(3)` 与 `12/3` 被判成**等价** —— 一次实打实的错答放行。
现在要求左括号前面不能是数字/字母/右括号（那意味着乘法，不是分组）。
剥不干净没关系：落回 `uncertain`，交给结构检查那层。

### 分层判定（C 的引擎部分）

新增 `judgeDeterministic(question, candidate)` → `{ verdict, layer }`：

```
scalar      标量比较就够了（纯数字 / 分数 / ±∞ / 不存在）—— 精确有理数比较，最便宜
structural  标量比不了，但结构检查能判（表达式、需要采样）—— 与生成闸门同一套引擎
none        机器判不了 → 交给模型兜底
```

`verifyAgainstQuestion` 改为委托给它（原来就是"给判题用的统一入口"，只是判题没在用它）。
**`compare()` 本身一个字没改** —— 它是标量专用的，混进表达式采样会破坏
`1/6` 与 `2/12` 那批精确比较。tests 里专门钉了一条守卫。

---

## 3. 探针测试结果（D）

两套并跑，因为它们证明的是不同的事：

| | 打哪里 | 证明什么 | 结果 |
| --- | --- | --- | --- |
| `judge-probe-local.mjs` | 本地引擎（前后端同一份 `math-quality.js`） | 判定逻辑本身 | **已完成** |
| `run.mjs --reuse` | 线上后端 | 线上有没有真的换成这一版 | **待部署后跑** |

### 规模与方法

- 题源：`--reuse .workbuddy/smoke/2026-09-19T16-04-21-after-report/questions.jsonl`（41 道已放行题）
- 探针定义：**11 条错答**（空话、指责参考答案、离谱数字、`2\left(·\right)`、取负、`2(·)`、
  `2\cdot`、`2\times`、`\frac12·`、`+1`、`−1`）+ **6 条合法古怪写法**（整段套括号、双重取负、
  乘 1、`\frac12·2` 相抵、等价分数、小数近似）
- 生成 **395 条错答探针 + 171 条合法探针 = 566 条**（另有 131 条按构造规则跳过，跳过原因如实记录）
- 跳过规则写在 `probes.mjs` 顶部：不定积分对常数不敏感（`+1` 探针对不定积分是**正确**答案）、
  参考答案恒为 0 时取负/乘 2 还是 0。这两类必须跳过，否则会把"判对了"记成"误批"
- 跳过**不用判题入口**判断，用一个独立的取样器（不同的采样点）判"参考答案是不是恒为 0"。
  拿被检验的对象来决定探针真值就是自证 —— 引擎有 bug 时探针会被悄悄跳过，报告里只剩一个漂亮的 0

### 结果

| 指标 | 值 |
| --- | --- |
| **错答被放行 `wrong_answer_accepted`** | **0 / 395** |
| 错答被拒 | 141 / 395 |
| 错答落到模型（引擎判不了） | 254 / 395（其中 82 条是两条自然语言探针，按设计就不该机器判） |
| 合法写法被误拒 | 0（表上 4 条全部来自 §7 那道**参考答案写错**的题，判它 `not_equivalent` 是对的） |
| 探针构造上被跳过 | 131 条（错答 56 + 合法 75），原因逐条记录在报告里，不是从分母里消失 |

### `wrong_answer_accepted = 0` 是**结构性的**，不是抽样运气

线上 `correct: true` 只有一条来路：确定性引擎判 `equivalent`。
模型那条路经 `trustModelVerdict` 只采信 `not_equivalent`。
所以只要没有错答探针被判 `equivalent`，线上就一定是 0 —— 这一条在
`tests/smoke-matrix.cjs` 里用静态守卫钉死了，改坏了会红。

---

## 4. 确定性覆盖率增益

`compare()` 这次没动，所以"Task #4 之前的旧口径"可以直接算出来，增益不是估的。

| 指标 | 之前 | 之后 | 变化 |
| --- | --- | --- | --- |
| 错答探针由确定性引擎判死 | 13 / 395（3.3%） | 141 / 395（**35.7%**） | **×10.8** |
| 错答探针落到模型 | 382 / 395（96.7%） | 254 / 395（64.3%） | −32.4pp |
| 合法探针由确定性引擎判死 | 13 / 171（7.6%） | 81 / 171（**47.4%**） | ×6.2 |
| 错答被放行 | 取决于模型（线上实测 6/40 误批） | **0（结构上不可能 > 0）** | — |

分层来源（41 道题的错答探针）：标量层 13 条、结构层 128 条、仍需模型 254 条。

按**题干能否被引擎解析**切开看，更能说明问题在哪：

| 分组 | 题数 | 错答探针 | 被引擎判死 | 覆盖率 |
| --- | --- | --- | --- | --- |
| 题干可解析 | 20 | 158 | 133 | **84.2%** |
| 题干引擎读不懂 | 21 | 155 | 8 | 5.2% |

单条探针（分母 41，跳过的不计）：`far_number` 25/41；`double_wrapped`、`sign_flip`、
`double_paren`、`double_cdot`、`double_times`、`half_shorthand` 各 17/37；
`plus_one`、`minus_one` 各 6/25（很多落在不定积分上被规则跳过）。

**AI 兜底率变化的正确读法**：错答探针的模型调用从 96.7% 降到 64.3%。
剩下的 254 条里，82 条是自然语言探针（必须问模型），
172 条是引擎够不着的形态 —— 其中 **44 条的参考答案本身就是标量**（见 §9 的下一步）。

---

## 5. AI Judge 权限收紧（C）

规则的唯一真源放在引擎里（`trustModelVerdict`），前后端共用同一条，避免两边口径漂移：

| 确定性层的结论 | 结果 |
| --- | --- |
| `equivalent` | `correct: true`（`method: deterministic`） |
| `not_equivalent` | `correct: false`（`method: deterministic`） |
| `uncertain` → 问模型 | 模型说 `not_equivalent` 且置信度 ≥ 0.9 → 采信为**错** |
| | 模型说 `equivalent` → **一律不采信**，返回 `uncertain` + `detail: equivalent_cannot_upgrade` |
| | 模型置信度不足 → `uncertain` + `detail: insufficient_confidence` |
| | 模型说 `canonical_suspected` → 作废题目，不怪学生（通道保留，未动） |

**为什么不对称**（写进代码注释了）：
错答被判对 → 学习数据被污染、自适应难度被带偏，而且用户永远不知道；
对答被判"暂时判不了" → 用户再提交一次。两边代价差一个量级。

`uncertain` 的处置：`trusted: false` → 调用方（`app.js`）在 `trusted !== true` 时**直接 return**，
不写 Ability / Topic Mastery / Review Queue，也不落历史记录。这一条是结构性的，
不是靠前端"记得别写"。文案改成 **「暂时无法可靠判断这个答案。这一次不计入统计，题目本身没有问题，可以再提交一次。」**

前端还加了一道同规则的最后防线：服务端若返回 `verdict: 'equivalent'` 但 `method !== 'deterministic'`，
浏览器一律当成"无法判定"，不判对。

---

## 6. 关掉审核员难度覆盖（E）

只改一处：`questionDifficultyLabel` 的取值链去掉 `displayDifficulty`，
变成 `calibratedDifficulty → provisionalDifficulty → requestedDifficulty`。

- 审核员的 `suggested_difficulty` **仍然照常记录**（后端照写 `q.displayDifficulty` 与
  `q.metadataCorrection`，服务端日志也在），只是不参与界面、也不参与 IRT（这两件事本来就没走它）
- 静态守卫补了一条：`displayDifficulty` 的解析字段不许被整个删掉 —— 它只该退出展示链
- 依据：线上 50 题评测量到计划 L12 显示成 L3/L4、L10 显示成 L5，而独立评估只漂一档。
  这说明是这条链路本身不可靠，不是"审核员更准"。等有真实作答数据能真正标定之后再启用

---

## 7. 意外发现：41 道已放行题里有 4 道参考答案是错的

这一节比判题的改动更要紧。全部有独立数值证据（不是靠引擎自己的判定）。

| 题 | 参考答案 | 实际 | 证据 |
| --- | --- | --- | --- |
| `limit-L8-3` | `-4` | **极限不存在**（两侧 ±∞） | `f(±h)` = ±902 / ±9002 / ±90000 …（h=1e-2/1e-3/1e-4），f ≈ 9/x |
| `limit-L10-2` | `-\frac{1}{6}` | **−∞** | f(h) = −1665 / −166652 / −16666527 → f ≈ −(1/6)/x² |
| `limit-L12-2` | `-\frac{2447e}{5760}` | **`+\frac{2447e}{5760}`（符号错）** | f(0.02/0.01/0.005/0.002) = 1.1326 / 1.1436 / 1.1491 / 1.1527 → 线性外推 1.15457 ≈ 2447e/5760 = 1.15480 |
| `integral-L4-2` | `3\ln|x-2|-2\ln|x-1|+C` | **`4\ln|x-2|-3\ln|x-1|+C`** | 部分分式 (x+2)/((x-1)(x-2)) = 4/(x-2) − 3/(x-1)；数值核对 F′=f 的点数：错答案 0/8，对答案 8/8 |

**为什么闸门没拦住前 3 道**：`estimateLimit` 对"两侧发散"和"精度悬崖"（4 阶抵消，
double 精度不够）**主动返回 null**，这是刻意的保守设计 —— 引擎宁可说判不了，也不猜。
但代价是：一个**有限数字**答案配一道**发散**的极限，引擎判不出矛盾，
`ANSWER_FAILS_VERIFICATION` 不触发，闸门放行。

`integral-L4-2` 是另一种情况：它以前被 `|` 挡住而无法判定（§2 改动二），
修好 `|A|` 之后引擎立刻抓出来了 —— **本次改动已经把它变成可检测的错题**，
重扫 41 道题确认：1 道被抓出（就是它），1 道从"判不了"变"能判"（`integral-L6-1`），**没有误伤其它 39 道**。

**对上一轮"Wrong approved 0/41"的修正**：那个 0 是**探测器覆盖率**的结论
（引擎能判的范围内没有错答被放行），不是"41 道答案都对"。现在必须这么读。

---

## 8. 是否需要重新部署

**需要，而且前后端都要。** 三份产物：

| 产物 | 内容 | 不部署的后果 |
| --- | --- | --- |
| 后端云函数 | `cloudbase/deepseek/index.js` + `math-quality.js` | 判题还是旧口径（`compare` only），错答照样漏给模型、照样被误批 |
| 静态前端 | `app.js` + `math-quality.js` | 浏览器里那道本地判题还是旧的。它跑在服务端之前，很多答案在浏览器里就被判掉了，轮不到服务端 |
| `api/deepseek.js` | 副本 | `check:sync` 会红（强制两份业务段逐字节一致） |

只部署后端也能堵住泄漏（浏览器判不了 → 交给服务端判），只是多一次往返；
只部署前端则不行（浏览器判不了的还是会落到旧服务端）。

**部署后必须做的一件事**：跑线上探针，确认响应里带 `judge_layer` 字段。
旧后端没有这个字段 —— 报告里会显式打出「部署指纹」段，
没有它就说明跑的还是旧版，那个 0 不能当验收依据。

```bash
node tools/smoke/run.mjs \
  --label v3-task4 \
  --reuse .workbuddy/smoke/2026-09-19T16-04-21-after-report/questions.jsonl \
  --judge-sample 20 --evaluate-sample 0
```

20 题 × (3 次重复 + 17 条探针) ≈ 340 条探针 / 400 次判题调用，落在 100–200 探针要求之上。

---

## 9. 残余风险与下一步

### 残余误批：0（但零的是"判题"，不是"题库"）

- 判题侧：`wrong_answer_accepted = 0`，结构性保证。**没有剩余误批。**
- 题库侧：§7 那 4 道题的参考答案是错的。学生写对答案会被判错（`not_equivalent`），
  写错答案反而被算对（标量自比）。这不是判题的锅，是**生成闸门的锅**。

### 三个待你确认的事项

**(1) 给闸门加"发散检测"（建议做，但不擅自做）**
规则草案：候选答案是**有限数字**，而两侧采样呈干净的发散特征
（|f(h/10)| / |f(h)| ≥ 5 连续两步）→ 直接判 `not_equivalent`。
能堵住 §7 的前 3 道。风险点：这是一个**肯定性断言**（断言极限不存在），
比"拒判"激进，需要更严的触发条件。会改变拒稿率，影响你计划的 200 题基准口径 —— 所以等你点头。

**(2) 标量层能不能算常量表达式（建议做，收益明确）**
现状：`2\left(\frac{1}{2}\right)`、`\left(\frac{1}{2}\right)`、`1\cdot(1)` 这些都判不了 ——
不是 parser 的问题，是 `atom()` 只认"裸标量"。这导致 172 条引擎够不着的错答探针里
**44 条的参考答案本身就是标量**，本可以不用问模型。
做法：`atom()` 在标量正则失败时，用 `tryParse` 试算一次"无变量常量表达式"。
风险：`compare()` 的语义会从"标量+精确有理数"扩到"标量+常量表达式"，
需要额外的浮点安全边界。这属于对判题核心的语义扩展，等你确认再做。

**(3) 题干形态支持（大工程，明确超范围）**
21 / 41 道题的题干引擎读不懂：`\sum`（5 道）、`\begin{cases}` 分段函数（4 道）、
隐函数求二阶导（4 道）、参数方程（3 道）、`\int\frac{dx}{…}`（4 道）。
这些题目前完全靠模型兜底，`wrong_answer_accepted` 为 0 靠的是"模型不能判对"这条规则，
而不是引擎。想真正覆盖，需要逐个形态做解析 —— 那是独立的一期。

### 下一步进 200 题基准的建议顺序

1. 先跑一次**线上 200 题**（`run.mjs` 不带 `--reuse`），拿到包含题库质量在内的基线；
   验收看两项：`Wrong approved`（题库侧）和 `错答探针被误批为对`（判题侧）都要是 0
2. 把 §7 那 4 道题的形态写进矩阵的固定样本（这类"有限答案配发散极限"是当前最弱的一格）
3. 再决定 (1)(2) 做不做 —— 它们都改变拒稿率/判定口径，会影响基准的可比性

---

## 附：本次没做的事（明确记下）

- 没有 push GitHub，没有 commit
- 没有为了让数字好看而放宽任何断言：`\frac1\pi`、嵌套绝对值、单竖线、多变量
  一律保持 `uncertain`，宁可判不了也不猜
- 没有把表达式判定塞进 `compare()`（有守卫测试钉着）
- 没有删除 `api/deepseek.js` / `cloudbase/.DS_Store` / 根目录 `.DS_Store` 这些冗余文件 ——
  它们进了版本库，属于清理项，但不在本期范围
