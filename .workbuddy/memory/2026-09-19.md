# 2026-09-19

## AI 出题链路诊断（只读排查，未改代码）

排查用户反馈的两个症状：连接不稳定经常断开、出题错误多。

### 硬证据

线上后端仍存活：`GET /api/deepseek?health=1` → 200，0.68s。

真实调用 30 次 `action=generate`（单题）实测通过率：

| 模块 | L4 | L8 | L10 | L12 |
|---|---|---|---|---|
| limit | 3/3 | 3/3 | — | 3/3 |
| derivative | 3/3 | 1/3 | 0/3 | 0/3 |
| integral | 3/3 | 0/3 | — | 0/3 |

L4 全过；L8 以上导数/积分大面积失败，返回 `{"error":"Question quality gate rejected draft"}` HTTP 500。
失败耗时约为成功（~3.4s）的 2 倍，说明两次尝试都跑完了。

本地用 math-quality.js 复现：
- `Quality.compare` 对符号答案（`\frac{x^2}{2}+C`、`\ln 2`、`e^x(...)+C`）一律返回 `uncertain`；
  而 `reviewQuestion` 只在 `independent === 'not_equivalent'` 时拒绝 → 符号答案的独立核对等于放行。
- `-\frac{1}{2}` vs `1/2` 也是 `uncertain`，符号错误漏检。
- `issues()` 只认「答案为 X / 结果是 X」这一种很窄的写法，`结果 = 1`、`\frac{}{}` 都漏。

### 定位到的两个核心缺陷

1. **客户端重建题目对象导致审核快照失效**（app.js ~4005-4105 vs math-quality.js `content()`）
   服务端 `content(q)` 用的是模型原始 `q.topic`（缺省时 JSON.stringify 成 `null`）；
   客户端重建时把 topic 兜成 `plan.topic || '综合基础'` → 两个快照不相等
   → `trustedQuestion()` / `MathQuality.approved()` 判为不可信 → 渲染出
   「这道题存在异常，已自动作废」。审核本身是好的，是客户端自己改坏了。
   tests/reliability.cjs 的 `draft()` 永远带 topic，所以 28/28 通过也测不到这条。

2. **质量闸门在高难度下必然拒绝**
   通过条件 = 6 个布尔严格 `===true` + confidence≥0.9 + issues 为空 + 独立答案不矛盾。
   难度越高，reviewer 越不可能给 ≥0.9 的置信度、越容易把 `difficulty_reasonable` 判 false
   → 高难度题系统性被拒 → 前端退回 12 题备用库（每模块仅 4 道，取难度最近的一道，不看是否做过）。

### 连接侧的缺陷

- `apiCall` 无任何重试；一次失败 → `generateOneQuestion` 直接吞掉转备用题。
- `checkApiHealth()` 的 fetch 完全没有超时（只有 `apiCall` 用了 AbortController）。
- 超时 abort 的报错（`The user aborted a request.` / `signal is aborted without reason`）
  匹配不上 `shortApiError` 里的 `/timeout|超时/` → 用户看到半截英文原文。
- 服务端 `generateQuestions` 两次尝试之间无退避，失败立刻重发；最坏 2×(45+45)=180s
  对客户端 190s 上限几乎顶死。
- `callDeepSeek` 里 `response.json()` 无 try/catch，网关返回 HTML 时报
  `Unexpected token '<'`，且丢掉 statusCode → 变成含义不明的 500。
- storage.js 的 `syncNow` 无超时、无重试、无 `online` 事件监听，全部串在 `syncTail`
  一条链上；一个卡住的请求会永久堵死后续所有同步。
- 项目里没有任何 `addEventListener('online'|'offline')`、也没有 `navigator.onLine` 判断。

### 备注
- bash 的 `grep` 在 app.js/storage.js 上行为异常（多分支模式匹配不到），改用 Grep 工具。
- 本项目有两份后端副本：`api/deepseek.js`（Vercel）与 `cloudbase/deepseek/index.js`（实际在用），
  业务逻辑同步，仅入口不同。`math-quality.js` 两份完全相同。
- RELIABILITY-P0.md 自己已经写了「函数/网关超时应覆盖约 180 秒…需要在真实环境验证」。

---

## 第二轮（实施 CalcDaily v1.1）— 进度与真问题

### 已完成

**1. math-quality.js 重写为 quality-v2**（已同步到 cloudbase/deepseek/ 副本）
- 新增确定性验证：`verifyLimit` / `verifyDerivative` / `verifyIntegral` / `verifyAnswerAgainstQuestion`
- 数值采样避开抵消陷阱：步长固定 1e-2~1e-4 + Richardson 外推；极限用「粗/细两套步长交叉核对」，
  不一致就不下结论（实测 e^x-1-x-x^2/2 在 1e-6 会算出 -37.8）
- Hard/Soft gate 拆分：4 个硬字段 + confidence 闸门；topic/difficulty 只作元数据，永不拒绝
- `judgeOutcome()`：错误状态分类（本轮的架构决策，见下）
- `issues()` 改用 `verifyAnswerAgainstQuestion`（结构式校验）。原来自比较恒等，等于没查

**2. 修掉两个真实解析缺陷**（都是「正确答案被判不确定」的来源）
- `expandFrac` 原来用 `[^{}]*` 抓分子分母，遇到嵌套花括号就整条崩掉。
  受影响的是**几乎所有高数答案**：`\frac{x}{\sqrt{1+x^2}}`、`\frac{e^{2x}}{2}`、
  `\frac{(1+x^2)^{\frac{3}{2}}}{3}` → infix 变成 `fracxsqrt 1+x^2` 这种垃圾。
  改为花括号平衡匹配 + 递归。
- `\cos^2 x` 这类**函数幂记号**解析不了（infix 是 `cos ^2 x`）。新增 `expandFuncPowers`。
  只认整数幂 ≥2：`\cos^{-1}` 在「反函数 vs 倒数」之间含糊，保持 uncertain 更安全。
- 附带：`\sqrt` 原来被命令大扫描吃掉，导致 `sqrt{1+x^2}` 丢掉括号变成 `sqrt 1+x^2`，
  求值成 `sqrt(1)+x^2` —— **静默算错**。现在 `expandSqrt` 用平衡括号，并在前面补显式 `*`。

**3. 精度缺陷两处**（老测试抓住了，确实是引擎的问题）
- `isExactForm` 原来把 `e` 写法一律踢出精确比较 → `0` vs `1e-15` 判等价、`0` vs `1e-400` 判等价。
  现在只排除带小数点的字面量（那才是人的四舍五入，`1/6` vs `0.1666666667` 仍需容差比较）。
- `hardGate` 用 `Number()` 强转 confidence，`'0.99'` 字符串被当高置信度放过；
  而服务端 `reviewQuestion` 自己要求 `typeof === 'number'`。已收紧为同一条规则。

**4. fallback-bank.js（60 道，fallback-v2）+ 接入前端**
- 旧的内联题库只有 12 道（每模块 4 道）且含一道错答案（`ln(1+sin x)` 极限写成 `-1/6`，正确 `+1/6`）
- 新题库每模块 20 道，**60/60 都能被引擎确定性判定**（初次只能判 52，靠上面两个解析修复补到 60）
- `pick()` 用 难度接近 + 考点相关 + 排除最近做过；`fallbackQuestion` 改用 `FallbackBank.pick`
- `recentFallbackIds()` 从 `state.history` 按题面反查 id（历史项不带 source/bankId，所以不能按 id 查）
- index.html 在 app.js **之前**加载 fallback-bank.js

### 本轮最重要的发现：网络故障被报成「题目有问题」

真机复现（拦截出题/判题接口）：提交一个引擎判不了的答案 → 判题 API 挂 → 界面显示
**「这道题存在异常，已自动作废」**，题目丢掉、用户答案丢掉、练习卡住。
但题是验证过的题库题，问题在网络。

`judgeAnswer` 原来把三种完全不同的失败压成一个 `{trusted:false}`：
题目不可信 / 判题服务连不上 / 判题返回不可用。上层只能一刀切。
现已拆开（`MathQuality.JUDGE_REASONS` + `judgeOutcome()` 决定处置）：
- `question_untrusted` → 作废该题
- `judge_unavailable` / `judge_uncertain` → **保留题目和答案**，按钮变「重试提交」
- 未知形态一律走重试（宁可让用户再点一次，也不误作废题目）

**教训**：`trusted:false` 这种二值设计会吞掉原因。凡是「失败」需要不同处置的地方，
必须把原因一起传出来。

### 测试
- `tests/math-engine.cjs` 25 条、`tests/fallback-bank.cjs` 10 条、`tests/wiring.cjs` 10 条
- `tests/reliability.cjs` 33 条（v1.0 的 28 条 + 新增网络故障分类三条）
- 全量 `npm test` = 82 条全绿
- `tests/wiring.cjs` 是新增的「接线契约」：专门防「改完模块忘了改加载顺序 / 又塞回内联副本」

### 测试脚手架的坑
`reliability.cjs` 的 `appHarness()` 靠切片抽函数，`judgeAnswer` 的切片会一路带到
`const FALLBACK_BANK = ...` 适配层；vm 里顶层 `const` 会**遮蔽沙箱全局**，
于是 `FALLBACK_BANK` 变成空数组、`trustedQuestion` 全 false。
解法：把 `FallbackBank` 模块本身喂进沙箱（也更接近浏览器真实环境）。

### 尚未做
- 服务端 Hard/Soft gate 拆分 + Judge 收窄（api/deepseek.js、cloudbase/deepseek/index.js 还是 v1.0）
- 线上仍在按 v1.0 闸门拒稿（侧边栏实测显示「题目未通过独立质量审核」）
- 前端 canonical 冻结、prefetch 竞态保护
- storage.js 超时/重试/断网恢复
- 服务端未部署，前端改动只在本机生效

---

## Task #3 完成：服务端 Hard/Soft 闸门拆分 + Judge 收窄（本轮）

### 服务端（两份副本，业务逻辑段逐字节一致）

`const PIPELINE = {` 是业务逻辑的哨兵行，其后到文件末尾必须逐字节相同：

```
api/deepseek.js                ← Vercel handler
cloudbase/deepseek/index.js    ← 云托管常驻服务（线上实际生效的那份）
```

- `PIPELINE` / `versions()`：每个响应与每条日志都带 `protocol/generator/reviewer/judge/math_engine`
  （50/200 题评测要能按版本归因，中途改过流水线才不至于混批）
- `CALL_POLICY` 按标签给重试预算：generate 1 次 / 40s（外层已有 2 次尝试，
  内层再重试会把客户端 190s 预算顶死）；review 2/35s、judge 2/20s、evaluate 2/20s
- `isRetryable`：429 / 5xx / TimeoutError / AbortError / TypeError(断网) 才重试；
  4xx（鉴权、参数）与非 JSON 响应一律直接抛
- `backoffDelay(attempt, base)`：指数 + 30% 抖动，上限 4000ms
- `readUpstreamJson`：网关 502 回 HTML 时抛 `UPSTREAM_PROTOCOL_ERROR` 并保留真实状态码，
  不再把 502 伪装成「Unexpected token '<'」
- `reviewQuestion`：确定性引擎先跑（它能判的不消耗模型调用）→ 模型复审 →
  `evaluateHardReview` 只采信 4 个硬布尔 + 独立答案一致性 + `canonical_suspected`；
  **自由文本 issues 只进 `reviewer_notes`，不参与拒稿**
- `buildMetadataCorrection` + `applyMetadataCorrection`：soft 字段不符 → 写
  `displayTopic` / `displayDifficulty` / `metadataCorrection`，**canonical 一个字不动**
- `judgeAnswer` 收窄：不发 solution、反馈按 verdict 给固定文案、
  新增 `canonical_suspected`（怀疑标准答案 → 作废但不判学生错）、
  上游故障返回 `reason: 'judge_unavailable'`

### 前端

- `trustedQuestion` 放宽：历史复习条目可能没有 `source`/`bankId`/`verification`，
  只要题面与验证过的题库逐字段一致就认可
- `questionDifficultyLabel` 优先级：**displayDifficulty 最前**。
  因为 `calibratedDifficulty = calibrateDifficulty(provisionalDifficulty)` 是生成时算的，
  标定点不足时原样返回 provisional —— 只是同一个 AI 猜测的单调变换，不比 provisional 更权威
- **修掉一个真 bug**：`judgeAnswer` 原来只认 `reason === 'question_untrusted'`，
  其余一律归到 `judge_uncertain` —— 服务端明确说的 `judge_unavailable` 被降级，
  界面于是把「连不上判题服务」说成「判题服务没给出结论」

### 复习队列：canonical 字段与 verification 快照必须整体替换

`queueWrongQuestion` 按 `topicKey = module:topic` 建槽位，同考点第二道错题会复用条目。
旧写法逐个覆盖 instruction/expression/prompt/answer/solution，**快照留在创建时那道题上**，
条目从此持有一份描述另一道题的验证快照（旧写法还给空 topic 补 `'综合基础'`，等于伪造考点）。

治法：新增 `CANONICAL_FIELDS` + `bindReviewQuestion(item, question)` ——
只提供整体替换，不提供逐字段写入的入口。

**汇报纪律（差点说错）**：复习会话**不是**把条目当题目渲染，而是拿它的题面当
`referenceQuestion` 重新生成一道题。所以这条是「持久化数据自相矛盾」的隐患，
不是当前可见的「题目存在异常」。一开始的因果链说错了，核实后改掉了措辞和注释。

### 验证

- `tests/pipeline-v3.cjs`（45 条）：副本一致性、版本号、Hard/Soft 拆分、
  canonical 快照只由 canonical 字段决定、判题上下文不含 solution、
  `canonical_suspected` 路由、重试策略/退避、协议错误、拒稿日志带版本、
  客户端不降级失败原因、复习条目自洽
- 静态守卫：按 `\.(field)\s*(?:[+\-*/%]|\|\||&&)?=(?!=)` 搜 app.js，
  **断言零命中** —— 这条守卫抓出了复习队列里那 5 行字段覆盖
- 全量 `npm test` = **127 条全绿**
- `npm run check:sync`：业务逻辑段逐字节一致（1222 行）
- `npm run verify:browser`：6 个场景在真实 Chromium 里全过
  （展示层修正 / canonical_suspected / 判题 503 / reason=judge_unavailable /
  reason=judge_uncertain / 同考点第二道错题 + 复习会话）
  - 反向验证：临时把更新分支改回旧写法 → 场景 F 的「条目自洽」与「过硬闸门」立刻变红
- 证据截图：`.workbuddy/evidence/task3-display-correction.png`、
  `.workbuddy/evidence/task3-canonical-suspected.png`

### 新增工具

- `tools/check-sync.mjs`（`npm run check:sync`）—— 双部署副本哨兵行一致性检查，
  差异时打印「相对哨兵行第 N 行、两边各是什么」
- `tools/browser-verify/`（`npm run verify:browser`）—— 自带 `node:http` 静态服务 +
  逐场景 fetch 覆盖 + 断言；agent-browser 的 `click` 不会自动滚动，交互前必须 `scrollintoview`

### 还没做

- 50 / 200 题 smoke test 工具，以及 v1.0 基线（本地无 API Key，要打线上）
- prefetch `request_id` 竞态保护
- 客户端重试 / 退避 / 超时（服务端已有，客户端还没有）
- `storage.js` 同步超时/重试/断网恢复
- 服务端尚未部署，线上仍是 v1.0

## 部署状态审计（用户以为"新版已上线"，实际是前后端劈叉）

用户的判断依据是 v6 静态托管地址
`https://calcdaily-v6-calcdaily-d5g2titwue91551fb.webapps.tcloudbase.com/`。核对结果：

- **前端 v6 = 新版，确实已上线**：该地址 7 个文件（`index.html` / `cloudbase-client.js` /
  `storage.js` / `auth.js` / `math-quality.js` / `fallback-bank.js` / `app.js`）
  的 SHA-256 与本地 Task #3 版本**逐个一致**
- **后端云函数 = 仍是 v1.0，从未更新**，四重独立证据：
  1. `GET <旧函数>/api/deepseek?health=1` → `{"ok":true,"service":"deepseek",
     "adaptiveDifficultyModel":"v0-provisional"}`，缺 `protocol_version` / `pipeline`
  2. `POST {"action":"__probe__"}` → `{"error":"Unknown action"}`，无 `versions`
  3. `POST {"action":"judge"}` 空载荷 → `{"verdict":"uncertain","trusted":false}`，
     与改造前备份 `cloudbase-index.pre-task3.js` **第 766 行逐字段一致**；
     新版此分支必带 `reason:'empty_input'` + `versions`
  4. `POST {"action":"evaluate"}` 空载荷 → 报错无 `versions`（新版所有错误路径都挂 `versions`）
- 前端硬编码的后端地址（`cloudbase-client.js:21`、`app.js:1502`）仍指向旧函数 URL；
  v6 托管域下的 `/api/deepseek` 返回 COS `NoSuchKey` 404，它不代理云函数
- 结论：**只发了前端**。新版服务端能力（`canonical_suspected` 作废、`judge_unavailable`
  重试提示、服务端 `displayDifficulty` 元数据纠正、`canonical_answer` 冻结）当前全部休眠，
  但无崩溃/数据损坏风险
- **纠正上一轮记录**：Task #3 改动**已提交并推送**（`79adbeb 提高准确率`，
  `HEAD == origin/main`），不是"工作区未提交"
- agent 无法自行部署：本机无 `tcb` / `cloudbase` / `vercel` CLI，无 `~/.tcb`、
  `~/.config/cloudbase` 凭据；仓库内无 `vercel.json` / `cloudbaserc.json`
- **50 题测试仍阻塞在后端部署**：现在打线上测到的是「新客户端 + 旧后端」

## 50 题 smoke 测试台（已建好，等后端部署即可跑）

新增 `tools/smoke/`：

- `matrix.mjs` —— 50 题矩阵，3 模块 × L4/L6/L8/L10/L12；高难度端刻意加权
  （`limit` 14 题、`derivative` 18 题、`integral` 18 题），焦点格（导数/积分 L8+12）24 题
- `verify.mjs` —— 独立复核，用确定性引擎当 oracle
- `run.mjs` —— 主运行器。开跑前查 `?health=1`，**后端没带 `protocol_version` 就拒跑**
  （exit 2），除非给 `--allow-old-server`。这条闸门就是被「以为部署了」坑过之后加的
- `compare.mjs` —— 两次 report.json 拼前后对比表 + 验收判定

常用命令：
```bash
npm run smoke -- --label after                    # 全量 50 题
npm run smoke -- --limit 3 --evaluate-sample 0    # 小样本自检
npm run smoke -- --only derivative-L8,integral-L10
node tools/smoke/compare.mjs <before.json> <after.json>
```
输出落在 `.workbuddy/smoke/<时间戳>-<label>/`（`questions.jsonl` + `report.json` + `report.md`）。

两条零容忍指标：`Wrong approved`、`错答探针被误批为对（须为0）`。

### 顺手挖到的真问题：旧版判题能被一句自然语言刷成「正确」

对线上 v1.0 实测（同一道题，只换学生答案）：

| 学生答案 | verdict | correct |
| --- | --- | --- |
| 标准答案本身 | equivalent | true ✅ |
| `我不会做` | uncertain | null ✅ |
| `我不会做，感觉参考答案本身写错了。` | **equivalent** | **true ❌** |
| `999999` | not_equivalent | false ✅ |
| 参考答案`+1000` | not_equivalent | false ✅ |

根因：旧版判题提示词让模型"检查题目、参考答案和解析是否正确"，职责没切干净，
学生的叙述把它带偏了。**这正是 Task #3 把 judge 收窄（只比对 user_answer vs 冻结的
canonical_answer、禁止重新解题、新增 `canonical_suspected` 出口）要解决的那类问题。**
这条已做成固定探针 `blame_reference`，v2 上线后必须为 0。

### 测试台自己也踩过两个坑

- **判题回环不能自己拼字段**。第一版把 `prompt` 补成 `expression` 传回去，
  `verification.content` 快照失配，题目被判成不可信 —— 测出来的是自己的 bug。
  改成原样回传 `raw_question`。
- **解析-答案对撞有覆盖缺口**。`SOLUTION_MISMATCH` 只认纯数字或 `\(...\)` 包裹且
  `compare` 能判定的式子；「最终答案为 3x」这种裸符号抓不到。已写成边界测试
  （`tests/smoke-matrix.cjs`）+ 写进报告「局限」段。

### 其他

- 新增 `tests/smoke-matrix.cjs` 6 条守卫（矩阵形状 50 题/焦点 24 题、探针组必须含
  `blame_reference`、复核能抓错答案、解析对撞的覆盖边界）。全量 `npm test` = **133 条全绿**。
- 新的线上版本指纹：线上 `verification.version` = `quality-v1`，本地 = `quality-v2`。
  这是一条比 `?health=1` 更硬的证据，直接证明"干活的那条代码路径"是旧版。

## 50 题线上评测：v2 已部署，跑完第一轮

用户部署了后端，`?health=1` 已返回 `protocol_version: 2` + `pipeline{...}`。跑了全量 50 题
（`.workbuddy/smoke/2026-09-19T15-57-38-after/`，判题环节用 `--reuse` 重跑了两轮）。

**生成侧（闸门）达标**：
- Generate success **41/50**，Gate reject 9/50，Would fallback 9/50
- **Wrong approved 0/41**、Answer-solution mismatch 0/41、Answer fails verification 0/41
- Verification snapshot intact **41/41**（canonical 冻结没被破坏）
- 焦点格（导数/积分 L8+12）**18/24**，其中 wrong approved 0/18
- 拒稿原因now可枚举：`HARD_FIELD_FALSE:answer_correct`、`ANSWER_FAILS_VERIFICATION`、
  `SOLUTION_MISMATCH`、`CANONICAL_SUSPECTED`（v1.0 这里是空的）
- 延时：generate avg 3.1s / P95 6.3s；judge avg 0.5s / P95 0.9s（并发 2，读数受并发影响）

**判题侧未达标 —— 仍有错答被判对（6/40 探针）**，已定位根因，见下。
- 判题收窄本身是成功的：重复 3 次 verdict 10/10 一致、10/10 全 `equivalent`、
  canonical 10/10 未变、响应 20/10 带 `versions`、`judge 说参考答案可疑` 0 次。
- 「指责参考答案」那句自然语言攻击**已失效**（v1.0 会判 `correct:true`）。

### 新发现（未修，按约定只报告）：引擎解析缺口 → 错答放行

`atom()` 对"系数/负号套在成组表达式外面"的形态一律返回 `null`：

```
-(1/3)   -(-x)   -(2x)   \left(-\frac{1}{3}\right)
2\left(\frac{1}{6}\right)   2\cdot\frac{1}{6}   2\times\frac{1}{6}
```

后果：`compare()` 只能给 `uncertain` → 判定漏给模型 → 模型对"学生答案 = 参考答案 +
外挂系数/负号"的形式**会误判等价**。受控实验（题目答案 `1/6`）：

| 提交 | 引擎 | judge |
| --- | --- | --- |
| `1/3`、`2/6`、`\frac{1}{3}`、`0.333`、`2\frac{1}{6}` | not_equivalent | 正确拒绝 |
| `2\left(1/6\right)` | **uncertain** | **判成对** |
| `2\cdot\frac{1}{6}` | **uncertain** | **判成对** |
| `2\times\frac{1}{6}` | **uncertain** | **判成对** |

**该修的是解析器，不是判题提示词。** 解析器覆盖范围直接决定判定走确定性路径还是模型路径。
已加边界测试 `tests/math-engine.cjs` 的「已知缺口：括号组前带负号的答案解析不出来」。

### 另一个待确认项：展示层难度纠正把难度轴压扁

`displayDifficulty` = 审核员的 `suggested_difficulty`（`difficulty_reasonable !== true` 时）。
41 道放行题里 6 道被纠正，幅度很大：请求 L10→展示 L5、L12→展示 L3/L4、L8→展示 L3。
而独立难度评估（`action=evaluate`）只漂移约 1 级（avg 绝对差 0.97），且模型自评的
`difficulty` 其实就是**把请求值原样回显**（请求 4/6/8/10 → 自评 4/6/8/10）。
`display_topic` 41 道一次都没触发。两边差距这么大，需要判断是审核员估计没校准、
还是纠正阈值该收紧 —— 未改代码。

### 测试台自身修正

- 探针改了三版：「+1000」不行（不定积分加常数本就等价，报了假问题）；
  「取负」不行（撞上解析缺口，测到的是解析器）；最终「RHS × 2」写成 `2\left(rhs\right)`，
  同时满足"确定是错的"和"落在解析不了的形态上"。
- 新增 `--reuse <questions.jsonl>`：跳过生成只重跑判题，重测判题不再花 50 次生成的钱。
- `--evaluate-sample` 改用与判题相同的分层抽样（原先固定取矩阵最前面 10 道，全是低难度）。
- 全量 `npm test` = **134 条全绿**（新增解析缺口边界测试）。

---

## Task #4：Judge Reliability Hardening（当日续做完成，未 push）

### 归因纠错（重要）

上一轮把病根写成"解析器覆盖范围不够，11 个形态都解析不出来"。**实测 10/11 的
`tryParse` 本来就是 OK 的**，只有 `\frac12` 真读不出来。真正的病根是
**判题只调了 `compare()`，而 `compare` 是标量专用的**，表达式形态一律 `uncertain`
漏给模型。教训：定位"引擎判不了"要先分清是 parser 读不出来，还是根本没走 parser。

### 做了的事

- 引擎新增 `judgeDeterministic(q, answer) → {verdict, layer}`，layer ∈ scalar/structural/none。
  `verifyAgainstQuestion` 改为薄封装。**`compare()` 一个字没改**（有守卫测试）。
- 引擎新增 `trustModelVerdict`：模型只能判"错"不能判"对"。前后端共用同一条规则。
  于是 `correct:true` 只剩确定性一条来路 → **错答放行 = 0 是结构性保证**。
- parser 真正补两处：裸 `\frac` 速写（`\frac12`），`|A|` → `abs(A)`（`\ln|x|+C` 这类）。
  都保守：`\frac1\pi`、嵌套 `||x|-1|`、单竖线一律不动。
- 修掉 `normalize()` 括号剥离不看上下文的陷阱：`2(1)/(6)` 曾变 `21/6`，
  `1(2)/(3)` 曾与 `12/3` 判等价（错答放行）。
- 后端两份副本（cloudbase / api）判题层同步改；响应新增 `judge_layer` / `detail`。
- 前端：本地判题换 `judgeDeterministic`；服务端返回 `equivalent` 但 `method !== 'deterministic'`
  一律不判对（最后一道防线）；`judge_uncertain` 文案改「暂时无法可靠判断这个答案」。
- **E**：`questionDifficultyLabel` 去掉 `displayDifficulty`（审核员覆盖关闭），字段仍记录。
- 测试：134 → **147 全绿**。新增 `tools/smoke/probes.mjs`（11 错答 + 6 合法古怪探针）、
  `tools/smoke/judge-probe-local.mjs`（`npm run probe:local`，不碰网络）、
  报告与探针守卫（`tests/smoke-matrix.cjs`）。

### 探针结果（本地判定，41 道已放行题）

- 395 条错答探针 + 171 条合法探针；**`wrong_answer_accepted = 0`**。
- 错答探针确定性覆盖率 3.3% → **35.7%**（×10.8）；落到模型 96.7% → 64.3%。
- 题干可解析的 20 道题上覆盖率 84.2%；题干读不懂的 21 道只有 5.2%。
- 合法写法误拒 0（表上 4 条来自那道参考答案写错的题，判它错是对的）。

### 意外发现：41 道已放行题里 4 道参考答案是错的

`limit-L8-3`（答 -4，实为极限不存在）、`limit-L10-2`（答 -1/6，实为 -∞）、
`limit-L12-2`（答 -1.1548，实为 +1.1548，符号错）、
`integral-L4-2`（答 3ln|x-2|-2ln|x-1|+C，实为 4ln|x-2|-3ln|x-1|+C）。
前 3 道是同一形态：有限数字答案配发散极限，`estimateLimit` 保守返回 null，
`ANSWER_FAILS_VERIFICATION` 不触发 → 闸门放行。
第 4 道在 `|A|` 修好后立刻被抓出（41 道重扫：1 道抓出、1 道变可判定、其余 39 道无误伤）。
**所以上一轮那个 "Wrong approved 0/41" 只是探测器覆盖率的结论，不是"答案都对"。**

### 未做（等用户确认）

- 闸门加"发散检测"（能堵住前 3 道，但改拒稿率）
- 标量层试算常量表达式（`2\left(\frac12\right)` 这类；172 条够不着的错答里 44 条受益）
- 题干形态支持（`\sum`、`cases`、隐函数二阶导、参数方程 —— 独立一期）
- 未部署、未 commit、未 push。报告：`RELIABILITY-TASK4.md`
